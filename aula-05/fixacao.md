// 1. Qual parte da prática usou arrays?
Em nomes, notas e frequencias
console.log("================================")

// 2. Qual parte da prática usou estrutura de repetição?
na lista ( arrays),
console.log("================================")

// 3. Onde vocês usaram operador aritmético?
em notas, frequencias e somas de media, resultado por aluno e media geral da turma.
console.log("================================")

// 4. Onde vocês usaram operador lógico?
no final para ser uma "alternativa" variavel.
console.log("================================")

// 5. Qual foi a função do if/else na prática?
função para ter uma escolha diferente.
console.log("================================")

 // 6. Qual foi a função do switch na prática?
 funciona como um menu de opções, dependendo do número colocado na variável opcao, ele mostrava uma informação diferente.
 console.log("================================")

 // 7. Por que os arrays de nomes, notas e frequências precisam ter a mesma quantidade de itens?
 Porque o código usa o mesmo número i pra acessar os três ao mesmo tempo.
 console.log("================================")

 // 8. O que aconteceria se esquecêssemos de usar .length no for?
  O length diz quantos itens tem na lista, sem ele o for não saberia quando parar.
  console.log("================================")

  // 9. Qual foi a parte mais fácil da atividade?
  criar os textos(arrays) e exibir os dados com console.log, pois era só escrever a lista e mostrar cada posição na tela.
  console.log("================================")
  
  // 10. Qual foi a parte mais difícil da atividade?
  montar o switch com todas as opções, pois precisava garantir que cada um  mostrasse a informação certa sem misturar os dados.